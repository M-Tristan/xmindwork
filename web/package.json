{
  "name": "web",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm run kill && vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "kill": "npx kill-port 3000"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "dayjs": "^1.11.3",
    "element-plus": "^2.2.5",
    "vue": "^3.2.25"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^2.3.3",
    "typescript": "^4.5.4",
    "vite": "^2.9.9",
    "vue-tsc": "^0.34.7",
    "kill-port": "^2.0.0"
  }
}